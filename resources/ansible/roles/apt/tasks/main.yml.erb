---
<% if version.nil? %>
- name: "apt install {{ name }}"
  apt:
    name: "{{ name }}"
    state: present
    update_cache: no
<% else %>
- name: "apt install {{ name }}={{ version }}"
  apt:
    name: "{{ name }}={{ version }}"
    state: present
    update_cache: no
<% end %>