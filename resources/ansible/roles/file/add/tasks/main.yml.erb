- name: Add "{{ content }}" in "{{ dest }}"
  lineinfile:
    dest: "{{ dest }}"
<% unless  data.fetch('insertafter', nil).nil? %>
    insertafter: '<%= data['insertafter'] %>'
<% end %>
    line: "{{ content }}"
